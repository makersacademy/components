<% component = "slider--with-media-components" %>

<div class="one-up-block">
  <div class="slider-block">
    <% graduates.each do | graduate | %>
    <div class="media-component media-component--reverse">

      <div class="container">

        <section class="media-component__section">
          <%= partial :"groups/title-block", locals: {
            parent: "media-component__section",
            modifiers: ["right"],
            titles: [
              {
                text: graduate[:name]
              },
              {
                modifiers: "subtitle",
                text: "#{graduate[:previous_job]} to #{graduate[:new_job]}, #{graduate[:cohort]}"
              }
            ],
            blockquote: { text: graduate[:testimonial] }
          } %>
        </section>
        <section class="media-component__section">
          <%= partial :"groups/image-block", locals: {
              modifiers: "full",
              images: [
                {
                  modifiers: ["portrait"],
                  src: graduate[:image],
                  alt: "A picture of #{graduate[:name]}"
                }
              ]
            } %>
        </section>
        <section class="media-component__section">
          <%= partial :"groups/cta-block", locals: {
              parent: "media-component__section",
              modifiers: ["right", "inline"],
              ctas: [
                {
                  modifiers: "horizontal",
                  text: "Read more",
                  link: graduate[:case_study_link]
                },
                {
                  modifiers: "horizontal",
                  text: "Read blog",
                  link: graduate[:blog_link]
                }
              ]
            } %>
        </section>

      </div>
    
    </div>
    <% end %>
  </div>
</div>