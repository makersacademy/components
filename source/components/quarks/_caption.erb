<% component = "caption" %>

<figcaption class="<%= component_classes(locals.fetch(:parent, nil), component, locals.fetch(:modifiers, [])) %>">
  <% bodies.each do | body | %>
    <%= partial "components/quarks/body", locals: {
      parent: component,
      text: body[:text],
      modifiers: body[:modifiers]
      } %>
  <% end %>
</figcaption>
